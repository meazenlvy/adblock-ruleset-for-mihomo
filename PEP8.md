# PEP 8 Python 编码规范快速对照表

## 1. 代码编排
- **缩进**：统一使用 **4 个空格**，禁止使用 Tab。
- **行长限制**：每行最大长度不超过 **79** 个字符。
- **换行**：
  - 括号（圆括号、方括号、花括号）内隐式换行。
  - 使用反斜杠 `\` 换行（尽量避免）。
  - 二元运算符应在换行后（PEP 8 推荐），或者行末。
- **空行**：
  - 顶层函数/类定义：**2 个空行**。
  - 类内部方法定义：**1 个空行**。
  - 函数内逻辑块：**1 个空行**（适度使用）。

## 2. 命名规范
| 类型           | 约定                          | 示例                  |
| :------------- | :---------------------------- | :-------------------- |
| **模块**       | 全小写，下划线连接            | `my_module.py`        |
| **包**         | 全小写，尽量不用下划线        | `mypackage`           |
| **类**         | 首字母大写 | `MyClass`             |
| **异常**       | 同类，结尾加 `Error` 后缀     | `ValueError`          |
| **函数**       | 全小写，下划线连接            | `my_function()`       |
| **变量**       | 全小写，下划线连接            | `my_var`              |
| **常量**       | 全大写，下划线连接            | `MAX_COUNT`           |
| **私有成员**   | 前缀单下划线 `_`              | `_internal_var`       |
| **类私有**     | 前缀双下划线 `__`              | `__private_method`    |
| **魔术方法**   | 前后双下划线 `__`             | `__init__`            |

## 3. 导入
- **顺序**：
  1. 标准库导入
  2. 相关第三方库导入
  3. 本地应用/库导入
- **分组**：每组之间用 **1 个空行** 隔开。
- **具体化**：推荐 `import os` 而非 `from os import *`。
- **位置**：通常放在文件顶部，在模块注释和文档字符串之后。

## 4. 空格使用
- **二元运算符**：运算符两边各加一个空格（赋值 `=`，比较 `==, <, >, !=`，布尔 `and, or`）。
  - *注意*：关键字参数或默认参数值中的 `=` 不加空格。
- **逗号**：逗号后加空格，逗号前不加空格。
- **括号**：括号内侧不加空格。
- **冒号**：切片操作 `x[:]` 不加空格，但在类型注解中变量后加空格 `var: int = 1`。

## 5. 注释
- **块注释**：`#` 后加一个空格，段落之间用 `#` 分隔。
- **行内注释**：与代码至少隔开 **2 个空格**，`#` 后加一个空格。
- **文档字符串**：
  - 所有公共模块、函数、类、方法都应写文档字符串。
  - 对于单行文档，使用 `"""This is a docstring."""` 结尾的 `"""` 与开头同行。
  - 对于多行文档，结尾 `"""` 独占一行。

## 6. 其他建议
- **比较**：
  - 始终使用 `is` / `is not` 判断 `None`（如 `if x is None:`）。
  - 永远不要用 `==` 进行布尔值比较（如 `if flag is True:`）。
- **字符串**：
  - 单引号 `'` 和双引号 `"` 保持一致，但同一个文件中应保持风格统一。
- **编程建议**：
  - 不要依赖微妙的 C 语言优先级规则，始终使用括号明确优先级。
  - 使用 `startswith()` 和 `endswith()` 代替切片进行前缀/后缀检查。
